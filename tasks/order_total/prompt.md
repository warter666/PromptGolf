# 任务：跨货币金额换算

实现 `solution.py` 中的 `convert(amount, from_currency, to_currency)`。

- 汇率数据与换算说明见同目录的 `rates.py`，以它为准
- 返回：`float`，金额保留两位小数
- 无需关心参数从哪里来，实现换算本身即可

`tests/` 目录下是公开测试，可用 `python -m pytest tests -q` 运行。
